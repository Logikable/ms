/* The one translation unit miniaudio compiles into.
 *
 * miniaudio ships as a single header that is a declaration everywhere it is
 * included and an implementation exactly where this macro is defined.
 */
#define MINIAUDIO_IMPLEMENTATION
#include "third_party/miniaudio/miniaudio.h"
